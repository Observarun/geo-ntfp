Fork of martaszk/geo_ntfp; GEP: non-timber forest products.

v2.0: Reorganises the orginal code (presently in archive/) into a modular format.
v2.1: debugging;
    hazelbean-based implementation of operations in ntfp_functions.py;
    price_preprocess.py added to bring nontimber_price_iucn.csv in desired format.

Create the environment using: conda env create -f environment.yml.